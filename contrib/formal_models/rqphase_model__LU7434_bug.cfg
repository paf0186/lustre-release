\* @model: rqphase_model
\* @lu: 7434
\* @constant: InjectBug7434 = TRUE
\* @expect: fail
\* @violated: EventuallyComplete
\* @description: Single UNREGISTERING phase - can't distinguish first from second attempt
\* Note (2026-09-10 validation): the cfg used to list AllThreadsTerminate under
\* PROPERTIES, which rqphase_model.tla never defines; TLC exited with a config
\* error and the runner misread that as the expected failure.  Removed so that
\* EventuallyComplete is actually checked (and is violated, as tagged).
SPECIFICATION Spec

CONSTANTS
    InjectBug7434 = TRUE

INVARIANTS
    PhaseIsValid
    PhaseNeverUndefined
    NoNestedUnreg
    UnregCounterValid
    UnregCounterConsistent
    CompleteIsClean

PROPERTIES
    EventuallyComplete
